import FaqClient from './FaqClient';

// 🛡️ MODO DINÁMICO FORZADO (Escudo anti-errores de build)
export const dynamic = 'force-dynamic';

export default function FAQPage() {
    return <FaqClient />;
}